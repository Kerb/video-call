[phases.setup]
nixPkgs = ["nodejs_18"]

[phases.install]
cmds = ["cd server && npm install"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "node server/server.js"

# Environment variables
[variables]
NODE_ENV = "production"
PORT = "3001"
