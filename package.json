{
  "name": "vibe-opencall2",
  "version": "1.0.0",
  "private": true,
  "description": "VideoCall — browser video calls. Root package is for tests only; deploy lives in server/ (see railway.json).",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "jsdom": "^26.1.0",
    "socket.io-client": "^4.8.3",
    "vitest": "^3.2.4"
  }
}
