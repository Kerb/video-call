# 📹 VideoCall — Браузерные видеозвонки

Приложение для видеоконференций через браузер с анонимным доступом по 6-значному коду комнаты.

## Возможности

- ✅ Видеозвонки через браузер без установки ПО
- ✅ Анонимный доступ по 6-значному коду комнаты
- ✅ Поддержка 2-5 участников
- ✅ Текстовый чат в реальном времени
- ✅ Демонстрация экрана
- ✅ Управление камерой и микрофоном
- ✅ Выбор устройств (камера/микрофон)
- ✅ Адаптивный UI для мобильных и десктопных браузеров
- ✅ Автоматическое переподключение при разрыве соединения

## Технологии

- **Фронтенд**: Vanilla JavaScript (ES6-модули), WebRTC API
- **Signaling-сервер**: Node.js, Express, Socket.io
- **STUN-серверы**: Публичные Google STUN

## Быстрый старт

### Требования

- Node.js >= 18 (для тестов и `npm run dev`; сам сервер запускается и на 16+)
- npm или yarn
- Современный браузер с поддержкой WebRTC (Chrome, Firefox, Safari, Edge)

### Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd <папка-проекта>
```

2. Установите зависимости сервера:
```bash
cd server
npm install
```

3. Установите зависимости клиента (опционально, для разработки):
```bash
cd ../client
npm install
```

4. Установите зависимости для тестов (в корне репозитория):
```bash
cd ..
npm install
```

### Запуск локально

1. Запустите сервер (в терминале 1):
```bash
cd server
npm start
```
Сервер запустится на `http://localhost:3001`

2. Запустите клиент (в терминале 2):
```bash
cd client
npx serve .
```
Клиент будет доступен на `http://localhost:3000`

3. Откройте `http://localhost:3000` в браузере

### Использование

1. **Создать комнату**: Нажмите "Создать комнату" → скопируйте код комнаты → отправьте собеседнику
2. **Войти в комнату**: Введите 6-значный код → нажмите "Войти в комнату"

## Структура проекта

```
vibe-opencall2/
├── client/                 # Фронтенд (статические файлы без сборки)
│   ├── index.html          # HTML-разметка
│   ├── styles.css          # Стили (адаптивные)
│   ├── main.js             # Точка входа
│   ├── webrtc.js           # WebRTC логика
│   ├── socket.js           # Socket.io клиент
│   ├── ui.js               # UI компоненты
│   ├── chat.js             # Чат
│   ├── name-generator.js   # Генератор имён
│   └── package.json
├── server/                 # Signaling-сервер
│   ├── server.js           # Express + Socket.io (обработчики событий)
│   ├── rooms.js            # Логика комнат: коды, участники, очистка
│   ├── nixpacks.toml       # Сборка для Railway (Nixpacks)
│   ├── .env.example        # Пример конфигурации
│   └── package.json
├── tests/                  # Тесты (Vitest)
│   ├── server/             # Юнит + интеграционные (реальный Socket.io)
│   └── client/             # jsdom + стабы WebRTC API
├── railway.json            # Конфиг деплоя Railway (rootDirectory: server)
├── vitest.config.mjs
├── package.json            # Зависимости для тестов
├── DEPLOY_RAILWAY.md       # Инструкция по деплою
├── DEPLOY_CHECKLIST.md     # Чеклист перед деплоем
├── REVIEW.md               # Отчёт ревью и план покрытия тестами
└── README.md
```

## Конфигурация

### Переменные окружения сервера

Сервер читает переменные из окружения. Файл `.env` автоматически **не** загружается
(dotenv не используется): локально передавайте переменные через shell,
на Railway — через раздел Variables.

| Переменная | По умолчанию | Назначение |
|------------|--------------|------------|
| `PORT` | `3001` | Порт signaling-сервера. На Railway задавать не нужно — платформа передаёт его сама |
| `NODE_ENV` | `development` | `production` — сервер дополнительно раздаёт статику клиента из `../client` |

ICE-серверы (STUN) заданы в `client/webrtc.js`. Переменные `CLIENT_URL`,
`STUN_SERVERS` и `TURN_*` сервером не читаются.

### TURN-серверы (для production)

TURN пока не поддерживается кодом (см. Roadmap). Для работы через симметричные
NAT (~10-20% случаев) после реализации понадобится свой TURN-сервер (например,
coturn), конфигурация будет передаваться клиенту через signaling.

## Деплой

### Всё на Railway (рекомендуется)

📖 Подробная инструкция: [DEPLOY_RAILWAY.md](DEPLOY_RAILWAY.md)

**Один деплой для всего:**
```bash
# Railway.app → New Project → Deploy from GitHub
# Railway автоматически соберёт и сервер, и клиент
# URL: https://your-app.up.railway.app
```

**Структура деплоя:**
```
┌─────────────────────────────────────┐
│    Railway Project (один)           │
│  https://your-app.up.railway.app    │
├─────────────────────────────────────┤
│  /server  → Node.js + Socket.io     │
│  /client  → Статические файлы       │
└─────────────────────────────────────┘
```

**Преимущества:**
- ✅ Один проект вместо двух
- ✅ Нет CORS проблем (клиент и сервер на одном домене)
- ✅ HTTPS автоматически
- ✅ Автоматический деплой при git push
- ✅ Бесплатный тариф: $5/месяц, 500 часов

### Альтернатива: VPS

Ubuntu/Debian с Nginx + PM2 + Let's Encrypt (см. [DEPLOY_RAILWAY.md](DEPLOY_RAILWAY.md))

## Тестирование

### Автоматические тесты (Vitest)

```bash
npm install            # один раз, в корне репозитория
npm test               # разовый прогон
npm run test:coverage  # прогон с отчётом покрытия
```

Покрытие: сервер (`rooms.js`, `server.js`) — юнит- и интеграционные тесты
с реальным Socket.io; клиент (`socket.js`, `webrtc.js`, `ui.js`, `chat.js`,
`name-generator.js`) — jsdom со стабами WebRTC API. `main.js` unit-тестами
не покрыт: модуль запускает приложение при импорте (нужен рефакторинг
с внедрением зависимостей).

### Ручное тестирование

1. **1-on-1 звонок**: Откройте два браузера/вкладки → создайте комнату → войдите
2. **Групповой звонок**: 3-5 участников → проверьте все видеопотоки
3. **Демонстрация экрана**: Нажмите "Демонстрировать экран" → проверьте у других
4. **Чат**: Отправьте сообщения → проверьте доставку
5. **Переподключение**: Закройте вкладку → откройте заново → введите код
6. **Мобильные браузеры**: Проверьте на iOS Safari и Android Chrome

### Проверка ограничений

- **Максимум 5 участников**: Попытка входа 6-го должна показать ошибку
- **Генерация имён**: Вход без имени → должно сгенерировать "прилагательное существительное"

## Известные ограничения

- Максимум 5 участников (mesh-топология)
- Нет TURN-сервера по умолчанию (может не работать за некоторыми NAT)
- Нет записи звонков
- Нет регистрации пользователей
- История чата не хранится: вошедший позже не видит старые сообщения
- Нет индикации активного говорящего

## Roadmap

- [ ] TURN-сервер для production
- [ ] Ограничение по времени звонка
- [ ] Кнопка "Поднять руку"
- [ ] Реакции (эмодзи)
- [ ] Пригласительные ссылки
- [ ] Статистика качества соединения

## Лицензия

MIT

## Вклад

PR приветствуются! Пожалуйста, создайте issue перед большими изменениями.
